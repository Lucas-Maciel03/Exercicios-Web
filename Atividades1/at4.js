function divisao(dividendo, divisor){
     console.log(`Resultado = ${dividendo / divisor} - Resto = ${dividendo % divisor}`)
     //usando lib Math.floor(dividendo / divisor)
}

divisao(10, 2)
divisao(6, 5)